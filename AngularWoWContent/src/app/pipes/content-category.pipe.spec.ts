import { ContentCategoryPipe } from './content-category.pipe';

describe('ContentCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
