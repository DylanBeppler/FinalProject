import { ContentCommentPipe } from './content-comment.pipe';

describe('ContentCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
